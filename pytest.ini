[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra -q
# Spread warnings from the environment surface rather than failing the suite.
filterwarnings =
    ignore::DeprecationWarning